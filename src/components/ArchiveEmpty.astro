---
import { withBase } from '@/lib/paths';
---

<section class="empty-archive ruled-panel" aria-labelledby="empty-heading">
  <p class="folio" aria-hidden="true">待刊</p>
  <div>
    <p class="eyebrow">ARCHIVE IN PREPARATION</p>
    <h2 id="empty-heading">公开知识页正在编校</h2>
    <p>当前没有可公开的会议内容。原始逐字稿不会进入站点；只有完成独立提炼、脱敏与证据标注的页面才会在这里出现。</p>
    <a class="text-link" href={withBase('/learning/', import.meta.env.BASE_URL)}>先了解学习路径</a>
  </div>
</section>
